import * as React from 'react'
import * as ReactDOM from 'react-dom'

interface FormProps {

}

const Form: React.FC<FormProps> = (props: FormProps) => {
  return (
    <form>Generic Form</form>
  )
}
